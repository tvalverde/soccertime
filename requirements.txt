Django==6.0.8
django-admin-sortable2==2.3.1
requests==2.33.0
requests-cache==1.3.3
beautifulsoup4==4.15.0
# The parser `futbolenlatv.py` asks BeautifulSoup for by name. It used to sit at the bottom
# of this file under the tooling, unpinned, which made it look like a development
# dependency — it is not, and moving it would break the scraper without the site noticing.
lxml==6.1.1
uvicorn==0.40.0
Pillow==12.3.0
whitenoise==6.12.0

# The REST API, its OpenAPI schema and the assets the documentation page renders with.
# All three are production dependencies rather than tooling: the schema is generated by
# the running site on request and the Swagger page is served by it, so an image built
# without them answers 500 on `/api/`.
djangorestframework==3.18.0
drf-spectacular==0.30.0
# Swagger UI's own JavaScript and CSS, served from this origin. The CDN copy the stock
# template loads cannot work here: `script-src` allows `self` and nothing else, and
# `test_csp.py` pins that no third-party host is ever listed.
drf-spectacular-sidecar==2026.8.1
