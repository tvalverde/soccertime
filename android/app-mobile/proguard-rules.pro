# Retrofit, OkHttp and kotlinx.serialization all ship their own R8 rules inside their
# artifacts, so nothing about the network layer is repeated here. This file exists for what
# this application adds on top of them, and is deliberately empty until it adds something.
